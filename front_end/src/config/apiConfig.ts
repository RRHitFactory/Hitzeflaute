/**
 * API Configuration
 * Centralized configuration for backend API endpoints
 */

// Set this to your backend server IP address
// Use "localhost" for development on the same machine
// Use your local network IP (e.g., "192.168.1.100") for mobile/other device testing
export const BACKEND_HOST = "192.168.1.56";
export const API_BASE_URL = `http://${BACKEND_HOST}:8000/api`;
